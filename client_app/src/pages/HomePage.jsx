import React, { Fragment } from "react";
import { Navbar, ProductContainer } from "../components";

const HomePage = () => {
  return (
    <Fragment>
      <Navbar />
      <ProductContainer />
    </Fragment>
  );
};

export default HomePage;
